yumi_hardware:
  ip_address: {
    type: string,
    description: "IP address of the yumi robot",
  }
  read_port_l: {
    type: int,
    default_value: 0, # should be removed once fix is merged
    description: "Port number used to read from the yumi robot",
  }
  write_port_l: {
    type: int,
    default_value: 0, # should be removed once fix is merged
    description: "Port number used to write to the yumi robot",
  }
  read_port_r: {
    type: int,
    default_value: 0, # should be removed once fix is merged
    description: "Port number used to read from the yumi robot",
  }
  write_port_r: {
    type: int,
    default_value: 0, # should be removed once fix is merged
    description: "Port number used to write to the yumi robot",
  }
